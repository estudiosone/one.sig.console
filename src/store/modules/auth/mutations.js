export default {
  set_isLogged(state, value) {
    // eslint-disable-next-line no-param-reassign
    state.isLogged = value;
  },
  set_signInError(state, value) {
    // eslint-disable-next-line no-param-reassign
    state.signInError = value;
  },
};
